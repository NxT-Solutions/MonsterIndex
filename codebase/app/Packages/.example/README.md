# Package Template

Copy this folder to create a new bounded-context package under `app/Packages`.

Suggested namespace pattern:

- `Packages\\<PackageName>\\...`

Keep Laravel delivery code (controllers/requests/resources) inside the package's `Http` folder.
